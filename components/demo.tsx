import { ScannerCardStream } from "@/components/ui/scanner-card-stream";

export default function DemoOne() {
  return (
      <ScannerCardStream
        // direction={1}
        // showControls={false}
        // showSpeed={false}
        // friction={0.95}
        // scanEffect="scramble"
      />
  );
}
